name: cicd
on: 
 workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
     - name: checkout code
       run: echo "code clone"
       
  build:
    runs-on: ubuntu-latest
    steps:
     - name: create container
       run: echo "docke ps"
       
    
    
